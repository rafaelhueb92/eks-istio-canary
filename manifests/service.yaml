apiVersion: v1
kind: Service
metadata:
  name: nextjs-service
  namespace: canary-app
  labels:
    app: nextjs
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nextjs
  type: ClusterIP